<%= form_with(model: vaccination, local: true) do |form| %>
  <% if vaccination.errors.any? %>
    <% vaccination.errors.full_messages.each do |message| %>
      <%= js add_gritter(error, title: "Your Company App Notification", sticky: false, image: :notice) %>
    <% end %>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, id: :vaccination_title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.submit 'Save Vaccination', class: 'btn btn-primary top-marg' %>
  </div>
<% end %>
