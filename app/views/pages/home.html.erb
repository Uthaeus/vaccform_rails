<div class="row">
  <div class="col-md-5">
    <% if !current_user.is_a? GuestUser %>
      <%= render 'user_home' %>
    <% else %>
      <%= render 'unregistered' %>
    <% end %>
  </div>
  <div class="col-md-7">
    <div class="form-card">
      <%= image_tag "vacc_main.jpg", width: '100%' %>
    </div>
  </div>
</div>
